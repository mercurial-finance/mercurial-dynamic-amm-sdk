import { createProgram } from '../../utils';
import { Connection } from '@solana/web3.js';

// export function simulateInitializePermissionedPool(connection: Connection, opt?: {
//   programId?: string
// }) {
//   const {ammProgram} = createProgram(connection, opt?.programId);
//   ammProgram.simulate.initializePermissionedPool({
//     accounts: {},
//
//   })
// }